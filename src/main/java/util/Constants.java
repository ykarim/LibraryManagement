package util;

/**
 * Holds default variables to be rewritten by properties file
 */
public class Constants {

    private final static int DEFAULT_PORT = 5609;
    private final static int DEFAULT_PASSWORD_MIN_LENGTH = 8;
    private final static int DEFAULT_PASSWORD_MAX_LENGTH = 20;

    public static int CUSTOM_PORT = DEFAULT_PORT;
    public static int CUSTOM_PASSWORD_MIN_LENGTH = DEFAULT_PASSWORD_MIN_LENGTH;
    public static int CUSTOM_PASSWORD_MAX_LENGTH = DEFAULT_PASSWORD_MAX_LENGTH;
}
